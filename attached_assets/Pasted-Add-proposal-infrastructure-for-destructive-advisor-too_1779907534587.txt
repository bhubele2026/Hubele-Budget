Add proposal infrastructure for destructive advisor tools. The new flow: a destructive tool returns a "proposal" instead of executing; the user clicks Confirm; a new endpoint re-runs the tool with the saved args. Pending proposals live in a new Postgres table so they survive restarts.

PIECE 1 — Add a new table to lib/db/src/schema/index.ts. Place it alongside advisorAuditLogTable:

export const advisorProposalsTable = pgTable(
  "advisor_proposals",
  {
    id: uuid("id").primaryKey().defaultRandom(),
    householdId: uuid("household_id")
      .notNull()
      .references(() => householdsTable.id, { onDelete: "cascade" }),
    actorUserId: text("actor_user_id").notNull(),
    toolName: text("tool_name").notNull(),
    args: jsonb("args").notNull(),
    // A human-readable preview of what the tool will do, generated by
    // the tool's previewer function. Shown in the confirmation card.
    summary: text("summary").notNull(),
    // Lifecycle: 'pending' (waiting for user), 'confirmed' (user clicked
    // confirm, tool will execute), 'cancelled' (user dismissed), 'expired'
    // (>15 min without action).
    status: text("status").notNull().default("pending"),
    chatMessageId: text("chat_message_id"),
    createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(),
    resolvedAt: timestamp("resolved_at", { withTimezone: true }),
  },
  (t) => ({
    householdIdx: index("advisor_proposals_household_idx").on(t.householdId, t.status),
  }),
);

Then push the schema:
  pnpm --filter @workspace/db push

PIECE 2 — Update artifacts/api-server/src/lib/advisorTools.ts:

a) Extend ToolDefinition with an optional previewer:

   /**
    * For destructive tools: generates the human-readable summary shown
    * in the confirmation card before execution. Receives the validated
    * input; should return a short description like "Add recurring bill:
    * Netflix, $15.99/month, day 14, category Streaming".
    */
   previewer?: (input: TInput, ctx: ToolContext) => Promise<string> | string;

b) In dispatchTool(), branch on riskTier === "destructive". When destructive:
   - Skip the audit-log insert and tool execution
   - Call tool.previewer(parsed.data, ctx) (default to JSON.stringify(args) if no previewer)
   - Insert a row into advisor_proposals with status='pending', the validated args, the summary, and ctx.chatMessageId
   - Return a NEW shape: { ok: true, toolName, proposal: { id, summary } } (no auditLogId yet)

   This means DispatchResult needs a new optional proposal field:
     proposal?: { id: string; summary: string };

c) Add a new exported function confirmProposal(proposalId, ctx). It must:
   - Select the proposal by id
   - Verify householdId matches ctx.householdId
   - Verify status === 'pending'
   - Verify createdAt within last 15 min, else mark expired and return error
   - Mark status='confirmed', resolvedAt=now
   - Re-dispatch via the tool's normal handler path (audit log + executed status, with original args). Return that DispatchResult.

d) Add a new exported function cancelProposal(proposalId, ctx). Similar guards. Marks status='cancelled', resolvedAt=now.

Drizzle imports needed: advisorProposalsTable from "@workspace/db".

PIECE 3 — Create artifacts/api-server/src/routes/advisorProposals.ts:

import { Router, type IRouter } from "express";
import { requireAuth } from "../middlewares/requireAuth";
import { confirmProposal, cancelProposal } from "../lib/advisorTools";
import { logger } from "../lib/logger";

const router: IRouter = Router();

router.post("/advisor/proposals/:proposalId/confirm", requireAuth, async (req, res): Promise<void> => {
  const { proposalId } = req.params;
  if (typeof proposalId !== "string" || !proposalId) {
    res.status(400).json({ error: "proposalId required" });
    return;
  }
  const ctx = {
    householdId: req.householdId!,
    householdOwnerId: req.householdOwnerId!,
    actorUserId: req.userId!,
  };
  try {
    const result = await confirmProposal(proposalId, ctx);
    if (!result.ok) {
      res.status(400).json({ error: result.error ?? "Confirmation failed" });
      return;
    }
    res.json({
      ok: true,
      toolName: result.toolName,
      summary: typeof result.result === "object" ? null : String(result.result),
      auditLogId: result.auditLogId,
    });
  } catch (err) {
    logger.error({ err, proposalId }, "advisor: confirm failed");
    res.status(500).json({ error: "Confirmation failed" });
  }
});

router.post("/advisor/proposals/:proposalId/cancel", requireAuth, async (req, res): Promise<void> => {
  const { proposalId } = req.params;
  if (typeof proposalId !== "string" || !proposalId) {
    res.status(400).json({ error: "proposalId required" });
    return;
  }
  const ctx = {
    householdId: req.householdId!,
    householdOwnerId: req.householdOwnerId!,
    actorUserId: req.userId!,
  };
  try {
    const result = await cancelProposal(proposalId, ctx);
    if (!result.ok) {
      res.status(400).json({ error: result.error ?? "Cancel failed" });
      return;
    }
    res.json({ ok: true });
  } catch (err) {
    logger.error({ err, proposalId }, "advisor: cancel failed");
    res.status(500).json({ error: "Cancel failed" });
  }
});

export default router;

PIECE 4 — Mount the router in artifacts/api-server/src/routes/index.ts:

   import advisorProposalsRouter from "./advisorProposals";
   // ...alongside the other router.use(...) calls:
   router.use(advisorProposalsRouter);

PIECE 5 — Also update the chat tool-call summary path to include a `proposal` field on tool calls. In artifacts/api-server/src/lib/advisor.ts, where toolCallSummaries is built, attach the proposal (if present) to the summary entry. The ToolCallSummary type needs:

   export interface ToolCallSummary {
     name: string;
     ok: boolean;
     summary: string;
     auditLogId?: string;
     proposal?: { id: string; summary: string };
   }

After all five pieces, run pnpm typecheck on the api-server package. Confirm clean. Do NOT restart the dev server yet — OpenAPI + UI come next.

Report back.