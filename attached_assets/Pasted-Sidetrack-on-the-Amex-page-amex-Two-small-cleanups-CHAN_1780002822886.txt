Sidetrack on the Amex page (/amex). Two small cleanups:

CHANGE 1 — Collapse the filter section by default.

Currently the page renders a filter block at the top with Search, From, To, Source, Category, Member, Reviewed. This eats vertical space I rarely use. Make it collapsed by default with a small "Show filters" toggle button to expand it. State for expanded/collapsed lives in component state (no need to persist across reloads — default-collapsed every visit is fine). When expanded, it looks exactly like it does today. When collapsed, only the toggle button shows.

Place the toggle button somewhere unobtrusive at the top of the page where the filters used to sit. Something like a small button with a filter icon and "Show filters" text, flipping to "Hide filters" when expanded. Match existing button styling on the page.

CHANGE 2 — Amex Ending Balance chart: start at today, extend forward.

The "ENDING BALANCE · TRAILING 12 MONTHS" chart currently shows Jun 2025 – May 2026. Change it to: start at today, extend ~12 months forward.

Pre-today: nothing shown (this is a forward-looking chart).
From today: a single solid line that starts at today's actual Amex balance. As future days pass and Plaid syncs deliver new balance points, the line grows weekly (Sun-Sat buckets, matching what the Chase chart uses).

On day one this chart effectively renders as just a single dot at today's balance. That's fine — it'll grow into a useful trend line over the coming weeks as more data lands.

NO forecast line. Credit card spending isn't reasonably forecastable, so we just show real data as it accumulates.

Update the chart caption: "ENDING BALANCE — trailing forward 12 months" or similar — clearly signal this is forward-looking, not historical.
Update the date range subtitle: "Today – [12 months from today]".

Don't break the per-card chip filter — when the user clicks Delta Gold / Blue Cash / Platinum, the chart still scopes to that card's balance specifically (just forward-looking now).

Reuse the BalanceTrendChart component changes from task #785 (Chase chart actual vs forecast) — that work already extended the chart to support the date-keyed multi-series shape with a today marker. This task uses the same component, just passing only a single actualFromToday series (no historicalActual, no forecastFromToday). If task #785 isn't merged yet, hold this task until it lands so we're not building against a moving target.

VERIFY:
- /amex shows the filter section collapsed by default with a "Show filters" button
- Clicking the button expands the filter section
- The Ending Balance chart starts at today (no pre-today history)
- The chart x-axis extends ~12 months forward from today
- A single solid line begins at today's balance
- Switching card chips (All / Blue Cash / Delta Gold / Platinum) re-scopes the chart correctly
- Per-card balance numbers above the chart are unchanged

Run typecheck. Skip new tests — smoke test only. Personal app.